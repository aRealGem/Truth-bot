# Project board

Lightweight kanban for scoped work. **Manual:** when a plan starts, move the row **Backlog → WIP**. When its PR merges, move **WIP → Done** and paste the merged PR link.

Plan files usually live under `.cursor/plans/*.plan.md` (Cursor); links below use that path.

## Backlog

Prioritized 2026-04-26 around accuracy and transparency (cost optimization is in budget after the SOTU fire). Tiers run P0 (do first) → P4 (deferred) plus a Blocked bucket. Items tagged `(NEW)` were added in this prioritization pass; everything else is the existing row prose, just relocated.

### P0 — Direct accuracy/transparency wins. Do these first.

Without these, "100% MRS non-empty" is a comforting lie — we know URLs got retrieved by the tool, but not whether models reasoned from them, and we have no current regression signal on accuracy. Adjacent: the triage tier — which decides which claims short-circuit and which escape to frontier — is currently making decisions on uncapped, possibly low-signal search results.

| Item | Plan / pointer | Notes |
|------|------------------|-------|
| **`(NEW 2026-05-04)` Force tool use on temporal-dismissal-prone calls (`tool_choice=required`)** | [`eval/sotu-2026/temporal-regressions-runbook.md`](eval/sotu-2026/temporal-regressions-runbook.md) | The 4-case temporal-regressions set scored 0/4 in its first live run (run `cbc335a1`) because Gemini and OpenAI dismissed temporally-anchored claims (events with future-but-real dates that have since occurred) as `Unverifiable` instead of running their search tools. Force tool use via `tool_choice=required` (Gemini `function_calling_config.mode=ANY`; OpenAI `tool_choice={"type":"function","function":{"name":"web_search_preview"}}` or equivalent) on temporal-dismissal-prone calls. Validate with regression-set rerun. **Pairs with the Gemini prompt revision row below; together they gate PR #10 un-draft.** |
| **`(NEW 2026-05-04)` Intra-family consensus tie-break** | — | When two models from the same vendor family disagree (Gemini Pro vs Flash; Claude Opus vs Sonnet vs Haiku), the current consensus voter lets a tied family-internal vote produce no signal, which contributes to the 0/4 regression-set scoring. Implement a tie-break: prefer the higher-tier model on intra-family ties (Opus > Sonnet > Haiku, Pro > Flash); document the rule in consensus rendering for transparency. Touch points: consensus engine in [`src/truthbot/verify/engine.py`](src/truthbot/verify/engine.py) (or wherever `_compute_consensus` lives) plus a per-family precedence map. Validate with regression-set rerun. |
| **`(NEW 2026-05-04)` Gemini temporal-dismissal prompt revision** | [`metrics/adapter_interpretability/gemini_temporal_spot_sample.md`](metrics/adapter_interpretability/gemini_temporal_spot_sample.md) | Gemini dismisses claims with future-but-real dates as `Unverifiable` even when those dates have passed at query time and the events did happen — see spot-sample doc. Revise the temporal-anchoring section of the Gemini system prompt in [`src/truthbot/verify/adapters/gemini.py`](src/truthbot/verify/adapters/gemini.py) to (a) state the current date explicitly, (b) instruct the model to verify rather than dismiss when a previously-future date is now past, (c) include an example of the failure mode. Validate with regression-set rerun. |
| **Frontier OpenAI/Gemini `web_sources` hallucination** | — | Even at frontier tier, OpenAI and Gemini emit URLs that fail HEAD validation 100% of the time and get stripped — 27/27 OpenAI and 58/58 Gemini in the SOTU fire (frontier `web_sources` non-empty rates: 15% OpenAI, 0% Gemini). The 2026-04-26 defensive backfill in `build_multi_verdicts` ([`src/truthbot/verify/adapters/base.py`](src/truthbot/verify/adapters/base.py)) saves the audit trail by populating `model_reported_sources` from `tool_retrieved_urls`, but the *visible* grounding shown in published reports stays empty for those two providers. Scope: investigate root cause (prompt formulation, tool-result extraction code path, or post-processing); decide on a strip-and-substitute policy that uses `tool_retrieved_urls` for the visible `web_sources` slot too — possibly only when fabrication rate exceeds a threshold so we don't paper over genuine attribution. |
| **OpenAI frontier `web_search_preview` activation rate** | — | The 2026-04-26 SOTU fire showed OpenAI at 0.69 tools/claim (frontier) vs xAI at 2.15 and Gemini at 3.62 — suspiciously low. Two hypotheses: (a) gpt-5.4 is being parsimonious because it has strong priors and only searches when uncertain (good behavior, just verify), or (b) `web_search_preview` isn't reliably firing because of preamble or tool-spec wording (bad). Scope: single-claim sanity test against a known-needs-search claim with verbose tool logging; if (b), tighten the tool-use preamble in [`src/truthbot/verify/adapters/openai.py`](src/truthbot/verify/adapters/openai.py); if (a), document and close. |
| **Grok triage tier uncapped tool calls (accuracy + cost — promoted from P4 2026-04-26)** | — | The 2026-04-26 frontier `max_tool_calls` cap (commit `6decc53`) covers `GrokAdapter._call_with_search` only. The cheap-tier triage adapter (`grok-4-fast`) is still uncapped at 8+ tool calls per claim, which may be diluting triage decisions with low-relevance search results. Triage decides which claims short-circuit vs. escape to frontier, so a noisy triage signal degrades both the cache decision and the audit trail for ~50% of claims. Cost data point preserved: triage was 76% of the $4.97 SOTU total ($3.79 xAI, all triage tier per [`metrics/run_summaries/258b5758-8e25-4bf0-8f34-63778d2f976e.json`](metrics/run_summaries/258b5758-8e25-4bf0-8f34-63778d2f976e.json)). Scope: extend `TRUTHBOT_GROK_MAX_TOOL_CALLS` (or a separate `_TRIAGE` variant) to the triage adapter's `_call_with_search` site in [`src/truthbot/verify/adapters/grok.py`](src/truthbot/verify/adapters/grok.py), mirroring the frontier change. ~10-line change. Re-validate with the same 10-claim transcript used for the frontier validation (run `146ee42a`); measure both triage-verdict agreement vs. frontier and cost-per-claim. **Pairs with P0 #1 (frontier WS hallucination) since both touch `grok.py`.** |

### P1 — Accuracy adjacent. Do after P0 unblocks.

Tier-2 attribution and dissent surfacing close out the "audit trail is honest" loop. The retry path protects against tail-failures that turn 5 verdicts into 5 `UNVERIFIABLE`s.

| Item | Plan / pointer | Notes |
|------|------------------|-------|
| **Bug C — triage-tier URL grounding** | — | OpenAI + Gemini triage strip 100% of model-reported URLs (24/24 and 31/31 in the 2026-04-25 calibration; pattern preserved in the 2026-04-26 SOTU fire). Triage runs a separate single-claim code path so the multi-claim grounding-prompt strengthening from 2026-04-25/26 doesn't reach it. Pointer: [`STATUS.md`](STATUS.md) "Explicitly still deferred" (2026-04-25) and "Explicitly still deferred (unchanged from yesterday)" (2026-04-26). Scope: either reuse the multi-claim grounding prompt at triage scale, or accept the strip and document why (triage verdicts are consensus inputs, not published, so the strip is an audit signal rather than a production failure). Worth revisiting only if we start surfacing triage citations downstream. |
| **Multi-claim chunk failure → single-claim retry (batch mode)** | [.cursor/plans/multi_claim_batching_ac006a9d.plan.md](.cursor/plans/multi_claim_batching_ac006a9d.plan.md) (Risks section) | _Sidecar/live paths closed by Phase E Grok/Gemini slice 2026-04-23 — ``BatchDispatcher`` and ``VerificationEngine.verify_bundles_batch`` now fall back per-claim when ``call_multi`` raises. This row remains open only for the native batch-API route._ When a multi-claim request errors, returns malformed/empty JSON, or returns fewer verdicts than claims, retry the missing claims as individual single-claim batch requests within the same run rather than marking all N as `UNVERIFIABLE no_response=True` (current v1 behavior). **Hard requirement: retry params must match the original multi-claim call byte-for-byte except for `max_tokens`/`max_tool_calls` scaling** — same `model`, same `system` prompt with `cache_control: ephemeral`, same tools list (`web_search_20250305` for Anthropic / `web_search_preview` for OpenAI), same evidence injection policy, same `max_evidence_per_claim`, same temperature/sampling. This preserves prompt-cache parity and keeps single- vs multi-claim quality/cost comparisons clean — without it, retries are silently drawn from a different distribution and muddy the very signal we're collecting this for. Implementation hint: detect inside `reconcile_run` right after `parse_provider_results` (look for verdicts with `no_response=True` and `batch_call_id` matching `*::multi::*`), rebuild a single-claim `BatchDispatcher.submit` against just those claims, splice results into `verdicts_by_claim`. Defer until after first batched SOTU run produces empirical failure-rate data. |
| **`(NEW)` Dissent prominence in published report** | — | When `consensus_strength` is `weak` or `none` (2/29 in today's run), the report's consensus line obscures which models said what — readers see a single verdict with no surfaced disagreement. Transparency-by-default would inline the per-model badges so the dissent is visible without clicking into the claim page. Scope: edit the consensus rendering in [`src/truthbot/publish/site.py`](src/truthbot/publish/site.py) to show a compact per-model verdict strip whenever strength != `strong`. |
| **111-claim full-transcript SOTU run** | — | One-time stress test against the speech tail. The 2026-04-26 29-claim cap covers all current code paths and matches the GPT 5.4 Pro reference set in [`eval/sotu-2026/reference.json`](eval/sotu-2026/reference.json). Pipeline's own extractor pulls ~111 checkable claims from the full speech, so a `--max-claims 0` run would catch extractor edge cases on the speech tail and stress-test multi-claim chunking past the reference subset. Expected $15-20 at current pricing (linear from the $4.97 / 29-claim baseline; less if triage cache hits compound). Pairs naturally with the regression-scoring item above. |

### P2 — Transparency UX. Small items, real reader-facing payoff.

These don't change verdicts but make the existing verdicts easier to interpret. Worth doing only after P0 lands so we're not polishing the wrong thing.

| Item | Plan / pointer | Notes |
|------|------------------|-------|
| **Alphabetize models by company** | — | Model lists (consensus tables, model-engaged chips, report claim model-detail cards) should sort alphabetically by **company/vendor name** first (Anthropic, Google, OpenAI, xAI…), then by model name within each vendor. Currently ordering appears to be insertion/adapter-registration order. Find the render sites (likely `site/assets/truthbot.js` + the Python site generator that emits claim/report HTML) and apply a stable vendor-then-model sort. |
| **Consistent-width verdict badges** | — | Verdict badges (TRUE / MOSTLY TRUE / MIXED / MOSTLY FALSE / FALSE / UNVERIFIABLE) currently size to their label length, which makes rows look ragged on report and claim pages. Pin them to a shared min-width (or CSS grid with equal columns) in `site/assets/styles.css` so every badge renders the same width regardless of label. |
| **`(NEW)` Source-diversity audit (one-time analysis pass)** | — | Are we citing the same 5-10 fact-checkers across every claim? Bias risk if so. Scope: count distinct domains across `tool_retrieved_urls` in [`metrics/batch_sidecar/258b5758-8e25-4bf0-8f34-63778d2f976e.jsonl`](metrics/batch_sidecar/258b5758-8e25-4bf0-8f34-63778d2f976e.jsonl) plus Anthropic batch results, surface top-20 domain frequencies, decide if the spread is healthy or needs source-broadening prompt work. ~20 min of analysis. |
| **`(NEW)` Confidence-label calibration check** | — | Do "High confidence" verdicts actually correlate with verdict agreement against ground truth? Cheap-ish to compute against the 29-claim reference: for each model, bucket its verdicts by self-reported confidence and compare to ground-truth match rate. Tells us whether the confidence signal is meaningful or noise — informs whether to surface confidence at all in the published report. ~30 min one-time analysis. |

### P3 — Operational / infra. No accuracy delta.

Real and useful but not the optimization target right now.

| Item | Plan / pointer | Notes |
|------|------------------|-------|
| **ExpressionPi deploy** | [STATUS.md](STATUS.md) Next Steps #1 | `TRUTHBOT_SITE_ROOT`, nginx `/truthbot`, rsync/cron. |
| **Absolute URLs for social + feed (`[SITE_URL]` substitution)** | — | Follow-up to [#4](https://github.com/aRealGem/Truth-bot/pull/4). At publish time (probably wired into ExpressionPi deploy) rewrite the relative `og:image` / `twitter:image` URLs in every page and resolve the `[SITE_URL]` placeholder in `site-test/feed.xml` to the real production origin. Without this, Twitter/Facebook/etc. scrapers resolve the image path against whatever origin they hit (raw.githack, Pages) instead of the canonical site, and the Atom `<link href>`s stay broken. Options: (a) post-publish rewrite pass in `SitePublisher` gated on a `TRUTHBOT_BASE_URL` env var, (b) emit absolute URLs directly from the generator when `TRUTHBOT_BASE_URL` is set, leaving relative as the local/dev fallback. Also consider adding `<link rel="canonical">` while we're in there. |
| **Truthy lie-detect "heart" light → red** | — | Truthy the mascot's chest/"heart" indicator should flash red when a lie is detected, even though the eyes already turn blue for sad tears on FALSE verdicts. Treat heart and eyes as independent channels: eyes encode mood (sad/blue on FALSE), heart encodes detection state (red on any deception signal). Update the SVG/CSS state machine in `site/assets/truthbot.js` (or wherever the Truthy animation lives) so heart fill swaps to red on FALSE/MOSTLY_FALSE verdicts independently of eye color. |
| **Bluesky v2 publisher** | [TODO.md](TODO.md) Phase 7 | `post_report` + remove strict xfail on `tests/test_bluesky.py` unconfigured case. |
| **`.env` API-key truncation pre-flight** | — | The 2026-04-22 evening blocker (terminal-paste-truncated API keys; see [`STATUS.md`](STATUS.md) "Pre-submission blocker") was fixed by re-pasting, not structurally — it can recur silently and burn the first request as a 401 before failing visibly. Scope: add a CLI pre-flight in [`src/truthbot/pipeline.py`](src/truthbot/pipeline.py) that length-checks each provider's API key against known formats (`sk-proj-*` ≈ 156 chars, `xai-*` ≈ 84 chars, `sk-ant-*` ≈ 108 chars, Gemini ≈ 39 chars) and fails fast with a friendly diff-against-expected message before any LLM request fires. |
| **`.cursor/plans/` archive convention** | — | Old/completed plans accumulate in `.cursor/plans/` with no archive convention; the directory listing doesn't reflect what's actually live work. Light cleanup: pick a convention (subfolder `.cursor/plans/archive/`, suffix like `*.done.plan.md`, or front-matter `status: done`) and apply it retroactively to plans whose work has shipped. Probably 30 minutes of file moves + a README pointer. |
| **truthbot costs pull** reconciler | — | Pull vendor-side usage/cost via **admin APIs** (not browser scraping): **Anthropic** Admin Usage & Cost API; **OpenAI** `/v1/organization/usage` and `/v1/organization/costs` (org admin key); **Google AI Studio** has no billing API equivalent — **Vertex** spend is reconciled via **Cloud Billing export → BigQuery**. **xAI:** no public billing API as of 2026-04-22; skip for v1 or treat console as manual (no Computer Use / credentialed dashboard automation). Write rollups under `metrics/vendor_costs/<provider>-<yyyy-mm>.json`. Join to `metrics/adapter_calls.jsonl` by `run_id`, `adapter_name`, day; emit `metrics/reports/reconcile-<yyyy-mm>.md` (estimated vs billed per adapter). |

### P4 — Cost (deprioritized 2026-04-26 in favor of accuracy/transparency).

| Item | Plan / pointer | Notes |
|------|------------------|-------|
| **Live-mode multi-claim fan-out (Phase E — Anthropic/OpenAI extension)** | [.cursor/plans/grok-gemini-live-claim-batching_01cccf96.plan.md](.cursor/plans/grok-gemini-live-claim-batching_01cccf96.plan.md) ("Phase E extension" section) | The Grok/Gemini slice of Phase E shipped on 2026-04-23 (see Done row). What's left: teach `AnthropicAdapter.call_multi` and `OpenAIAdapter.call_multi` to reuse their `build_multi_batch_payload` shape against the live endpoints (`client.messages.create` / `client.responses.create`) so `--mode live` benefits too. Both are ~30-line overrides. Deferred to preserve the current `--mode live` baseline for A/B cost comparison against the 29-claim SOTU run. |

### Blocked — vendor-dependent. Track for status changes; not prioritizable.

| Item | Plan / pointer | Notes |
|------|------------------|-------|
| **Gemini native batch transport** (blocked on Google) | `src/truthbot/verify/batch.py` | **Not an engineering task — waiting on Google.** `google.genai.batches` exists, but it's a Vertex AI-style GCS batch prediction service (GCS input/output buckets, separate service-account credentials) that does **not** support tool calls or GoogleSearch grounding. Grounding is only on synchronous/streaming calls today, so `supports_batch=False` is the correct setting, not a TODO. Current reality: Gemini participates in batch-mode consensus via the **live-sidecar path** (`GeminiAdapter.call()` runs live during submit with full grounding + context caching; verdicts spooled to `metrics/batch_sidecar/<run_id>.jsonl` and merged at reconcile). The only thing we'd gain from a real native batch is the ~50% discount Anthropic/OpenAI get — at the cost of losing grounding, which would tank verdict quality. Reopen when Google ships grounding in their batch tier (or when it's worth maintaining a separate Vertex + GCS credential chain for the price break). |
| **xAI Grok batch** | — | xAI has no Batch API as of 2026-04-22. Current design runs Grok live during `--mode batch` submit as a "sidecar" and spools verdicts to `metrics/batch_sidecar/<run_id>.jsonl`; poll merges them. As of Phase E (2026-04-23) the sidecar now folds claims via ``GrokAdapter.call_multi`` (one API call per 6-claim chunk) instead of one-per-claim, so the only remaining win would be the ~50% native-batch discount. Revisit when xAI ships batch + prompt caching. |

## WIP

_v2 (HydraMind / truth-bot v2) is tracked externally as **P67 / P96** on the cass-wip board; the rows below are the in-repo view._

| Item | Plan | PR |
|------|------|-----|
| **HydraMind cost model + proxy pricing audit (v2)** | [`eval/benchmarks/COST_MODEL.md`](eval/benchmarks/COST_MODEL.md) · [`eval/benchmarks/PROXY_PRICING_AUDIT.md`](eval/benchmarks/PROXY_PRICING_AUDIT.md) | [#20](https://github.com/aRealGem/Truth-bot/pull/20) (open) |
| **Rev-2 reliability — URL grounding + harness + audit (checkpoint)** | [`eval/sotu-2026/temporal-regressions-runbook.md`](eval/sotu-2026/temporal-regressions-runbook.md) · [`metrics/adapter_interpretability/strip_audit_2026-05.md`](metrics/adapter_interpretability/strip_audit_2026-05.md) | [#10](https://github.com/aRealGem/Truth-bot/pull/10) (draft). 18 commits, 786 tests passing. Verify-side: universal trust-when-fired URL grounding (`0b8a16f` + `ea10e34`), Gemini grounding-redirect resolution (`2e42909`), live Gemini adapter dep (`0c457ca`), pipeline correctness fixes (`9c85b45`, `ffd28a2`). Publish-side: run-manifest panel + degraded-consensus disclosure (`fab3a25`, roadmap [4]); audit-trail tier for model-cited unverified URLs (`f447fa7`); family-aware dissent flagging (`a006dd9`, roadmap [6]). Eval/harness: pinned 4-case temporal-regressions set (`399beb5`, roadmap [5]); `match_keywords` + harness-vs-substance findings, schema v2 (`230a629`). Diagnostics: `truthbot metrics tool-stats` subcommand (`6c0ec34`, roadmap [7]); arm-D/E/F probe artifacts (`f6175fb`, `7ef90bb`, `a3f46c6`). **Gating un-draft: the three `(NEW 2026-05-04)` P0 substance-track rows above** — first live regression run scored 0/4 due to those substance issues, not harness regression. |

## Done

### HydraMind v2 (2026-07)

| Item | Plan | PR |
|------|------|-----|
| **truth-bot v2 on HydraMind L2 — engine, invariants, Layers A/B (PCA proposer→critic→arbiter)** | P96.2 / P67 (external) · [`STATUS.md`](STATUS.md) 2026-07-08/09 | [#12](https://github.com/aRealGem/Truth-bot/pull/12)–[#16](https://github.com/aRealGem/Truth-bot/pull/16) |
| **P96.2.1 cost telemetry — proxy per-call cost + `$2` ceiling, validated live to the penny; named `label_mismatch` escalation criterion** | [`eval/benchmarks/COST_MODEL.md`](eval/benchmarks/COST_MODEL.md) | [#16](https://github.com/aRealGem/Truth-bot/pull/16), [#18](https://github.com/aRealGem/Truth-bot/pull/18) |
| **truth-bot client key + independent run ledger + DeepInfra rate/env docs** | [`metrics/spend_ledger/SCHEMA.md`](metrics/spend_ledger/SCHEMA.md) · [`eval/benchmarks/PROVISIONING.md`](eval/benchmarks/PROVISIONING.md) | [#17](https://github.com/aRealGem/Truth-bot/pull/17), [#19](https://github.com/aRealGem/Truth-bot/pull/19) |

### v1 pipeline

| Item | Plan | PR |
|------|------|-----|
| **Round 4 polish — Truthy audio fix, lens-labeled bars, Strict default, headline frames** | [.cursor/plans/round_4_polish_725cf2af.plan.md](.cursor/plans/round_4_polish_725cf2af.plan.md) · [`STATUS.md`](STATUS.md) 2026-04-30 session note (Round 4) | 2026-04-30 session. Four polish threads + two advisory answers. (1) Truthy audio actually plays now: `getCtx()` → Promise-returning `unlockAudio()`, `play*` accept a pre-resolved `ctx`, `speak()` is `.then(`-chained, queued first-gesture autoplay gained `pointerdown` so nav-link clicks no longer beat the oscillator schedule. Mirrored across embedded JS + standalone `truthbot.js`. (2) Each verdict bar (report panel + index card) self-labels with `Strict lens` / `Lenient lens` so the reader doesn't need to consult the chip. (3) Default editorial-lens flipped from Lenient to Strict; Strict-first DOM order in `_verdict_panel`, `_report_card`, `_toc`, status-bar chip, and the JS `DEFAULT_LENS` constant — non-JS clients now see Strict by default. Stored user preference still wins on revisit. (4) `% Truthy or better` promoted out of the stats grid into a new 2-frame `vp-headline-stats` block above the stats grid; new `% False or worse` joined it (formula: `(False + Falsey) / total_claims_including_unverifiable`). Stats grid reverted to `stats-4`. New X-in-circle icon, tinted left accent bars (`--v-truthy` / `--v-falsey`). 8 new/updated render tests + new [`tests/test_truthy_audio_unlock.py`](tests/test_truthy_audio_unlock.py) contract pin (`unlockAudio` Promise, `pointerdown` queue). 703 tests pass. Demo refreshed via `scripts/republish_site_test_from_cache.py --skip-rebuild` (no LLM calls). |
| **Round 3 polish — Truthy mute + % truthy stat + model panel insights** | [.cursor/plans/round_3_polish_fbd1f08c.plan.md](.cursor/plans/round_3_polish_fbd1f08c.plan.md) · [`STATUS.md`](STATUS.md) 2026-04-30 session note | 2026-04-30 session. Three threads bundled to share one demo refresh: (1) site-wide Truthy mascot mute toggle (`localStorage.truthy-mute`, default off, queued one-shot first-gesture autoplay; mascot tap = sticky toggle; Truthy fun page excluded) — fixes the "no sound on report pages" bug. (2) "% Truthy or better" lens-aware stat on the report verdict panel (5th tile, paired data-lens-axis spans, denominator includes Unverifiable per editorial). (3) New [`src/truthbot/publish/insights.py`](src/truthbot/publish/insights.py) generalizes [`eval/opus_vs_rest_scan.py`](eval/opus_vs_rest_scan.py) into a render-agnostic data layer; landing-page strip surfaces strongest pair / most-divergent / lenient-vs-strict bias spread + CTA to a new dedicated [`model-insights.html`](site-test/model-insights.html) deep-dive (per-model summary table, signed truthy-bias bars, 4×4 pairwise agreement matrix, top extreme-split cards, methodology footnote). 25 new tests across `tests/test_insights.py`, `tests/test_site_render_insights.py`, `tests/test_site_render_aggregates.py`. Demo refreshed via `scripts/republish_site_test_from_cache.py --skip-rebuild` (no LLM calls). |
| **5-bucket projection on aggregates + frontier-badge cleanup** | [.cursor/plans/5-bucket_everywhere_+_frontier_cleanup_150fadfa.plan.md](.cursor/plans/5-bucket_everywhere_+_frontier_cleanup_150fadfa.plan.md) · [`STATUS.md`](STATUS.md) 2026-04-29 session note | 2026-04-29 session. Round 2 of the projection work: the 5-bucket Truthy scale now drives every aggregate display (verdict-panel headline + ratio + bar, TOC mini-pills, index per-report cards, site-wide totals) — not just the per-claim headline pill. Lens chip swaps Lenient/Strict in lockstep across all surfaces via two patterns: per-pill text+class swap (extended via shared `.lens-pill` class) and paired-axis show/hide (`[data-lens-axis]` blocks). Per-model "frontier"/"batch" tier chip retired; methodology line "verified by N frontier language models" intentionally kept. 15 new aggregate render tests (`tests/test_site_render_aggregates.py`); existing 23 projection tests still green. Closes the P2 backlog row "Drop 'Frontier' from model detail cards" (was describing the same chip). |
| **5-bucket coarse-axis projection (Lenient default + Strict toggle)** | [.cursor/plans/5-bucket_coarse-axis_projection_ba2d0050.plan.md](.cursor/plans/5-bucket_coarse-axis_projection_ba2d0050.plan.md) · rubric: [`eval/sotu-2026/findings-review.md`](eval/sotu-2026/findings-review.md) Part H · empirical motivation: C11 + Rec #3 in same doc, validated against the 84 published claims (Lenient: +16pp strong consensus; Strict: no change) · [`STATUS.md`](STATUS.md) 2026-04-27 session note | 2026-04-27 session, commit `dc64ca0`. Models keep their 6-bucket rubric and prompt cache; consensus emits both projections; per-claim headline pill defaults to Lenient with a Strict toggle in the status bar; per-model strip stays 6-bucket for audit. Engine + projection unit tests + render tests landed. Round-2 expansion to aggregates landed 2026-04-29 (row above). |
| **FitnessScorer coarse-axis verdict-agreement metric + Run 5** | [.cursor/plans/fitnessscorer_coarse-axis_b49ee82d.plan.md](.cursor/plans/fitnessscorer_coarse-axis_b49ee82d.plan.md) · [`eval/sotu-2026/BENCHMARK.md`](eval/sotu-2026/BENCHMARK.md) Run 5 · [`STATUS.md`](STATUS.md) 2026-04-28 session note | 2026-04-28 overnight. `FitnessScorer.score(axis="fine"|"coarse_lenient"|"coarse_strict")` + `score_run.py --all-axes` re-score Run 4 on the 5-bucket Truthy scale. **Headline (counter to going-in hypothesis):** Strict lifts verdict-agreement +6.2pp (62.8% → 69.0%), Lenient drops it −2.4pp. Cause: reference set has 9 MISLEADING-family claims and zero EXAGGERATED; consensus has 6 Exaggerated and zero Misleading. Strict collapses both to Falsey and isolates real disagreement; Lenient pushes them onto opposite sides of Truthy/Falsey. **Architectural takeaway:** site keeps Lenient (reader-facing); regression scoring should default to Strict (reference-facing). Closes ≈14% of the −0.1377 fitness gap; remaining ≈86% is genuine quality drift. 25 new tests + 106/106 existing eval tests still pass; default `--axis fine` is byte-identical to Run 4. |
| **Reference-set regression scoring (P0 #3) — first scorecard, current baseline established** | [`eval/sotu-2026/score_run.py`](eval/sotu-2026/score_run.py) · [`eval/sotu-2026/BENCHMARK.md`](eval/sotu-2026/BENCHMARK.md) Run 4 · [`STATUS.md`](STATUS.md) 2026-04-27 session note | 2026-04-27 session. New CLI `score_run.py` joins extractions + sidecar + `claims.json` to feed `FitnessScorer`. Run 4 added as current baseline: fitness **0.5413** (Δ −0.1377 vs single-model 0.679 baseline; the gap conflates genuine drift, consensus-voting label drift, and the new "Models split" state). Three calibration gaps surfaced as follow-ups (Anthropic explanation persistence, parsimony recalibration, Anthropic-only Run 5 for clean A/B — partially addressed by 2026-04-28 coarse-axis Run 5). |
| **Pre-SOTU Grok max_tool_calls cap + multi-claim model_reported_sources backfill** | [.cursor/plans/pre-sotu-grok-cap-and-backfill_7b176093.plan.md](.cursor/plans/pre-sotu-grok-cap-and-backfill_7b176093.plan.md) · [.cursor/plans/fire-sotu-29-and-record_1604973b.plan.md](.cursor/plans/fire-sotu-29-and-record_1604973b.plan.md) | commits `6decc53` + `d21b2d1` (2026-04-26). Validated by 29-claim SOTU fire (run `258b5758`); −66% vs Phase E projection at $4.97 total. |
| **Multi-claim batching (batch mode)** | [.cursor/plans/multi_claim_batching_ac006a9d.plan.md](.cursor/plans/multi_claim_batching_ac006a9d.plan.md) · Phase E sidecar slice [.cursor/plans/grok-gemini-live-claim-batching_01cccf96.plan.md](.cursor/plans/grok-gemini-live-claim-batching_01cccf96.plan.md) | — _(Anthropic + OpenAI native batch shipped 2026-04-22; Grok + Gemini live-sidecar shipped Phase E 2026-04-23; defensive multi-claim backfill shipped 2026-04-26 commit `6decc53`. End-to-end exercised by SOTU 2026 29-claim fire, run `258b5758`. Live-mode Anthropic/OpenAI overrides remain open as a separate Backlog row — see "Live-mode multi-claim fan-out (Phase E extension)".)_ |
| **"Beta" badge next to version until 1.0** | — | — _(IS_BETA / BETA_BADGE_HTML / BETA_TEXT_SUFFIX in [`src/truthbot/publish/site.py`](src/truthbot/publish/site.py); wired into status bar, footers on index/report/claim/truthy/about, the about-page prose, `<meta name="generator">` on all four page shells, `FEED_XML_TEMPLATE` Atom generator, and the RSS 2.0 `<generator>` in [`src/truthbot/publish/rss.py`](src/truthbot/publish/rss.py). `.beta-badge` CSS added in section [18a] of the embedded stylesheet. Auto-disappears when `PIPELINE_VERSION` crosses 1.0.0 because the render sites concat `BETA_BADGE_HTML`/`BETA_TEXT_SUFFIX` which become empty strings.)_ |
| **Grok + Gemini live claim-batching (Phase E — Grok/Gemini slice)** | [.cursor/plans/grok-gemini-live-claim-batching_01cccf96.plan.md](.cursor/plans/grok-gemini-live-claim-batching_01cccf96.plan.md) | — |
| Social sharing infra, favicon, Atom feed, prompt-hash footer, src-tiers chip | — | [#4](https://github.com/aRealGem/Truth-bot/pull/4) |
| DataHoover evidence provider + cost optimizations + 2026-04-22 cost-table refresh | [.cursor/plans/datahoover-hook-plus-costs_02ead614.plan.md](.cursor/plans/datahoover-hook-plus-costs_02ead614.plan.md) · [.cursor/plans/cost-table-refresh-and-board_be12e335.plan.md](.cursor/plans/cost-table-refresh-and-board_be12e335.plan.md) | [#3](https://github.com/aRealGem/Truth-bot/pull/3) |
| Project board (kanban) | [.cursor/plans/cost-table-refresh-and-board_be12e335.plan.md](.cursor/plans/cost-table-refresh-and-board_be12e335.plan.md) | [#2](https://github.com/aRealGem/Truth-bot/pull/2) |
| Historical SOTU transcripts corpus (Nixon–Trump) | — | [#1](https://github.com/aRealGem/Truth-bot/pull/1) |
