# PCA seat rosters. Seats: proposer (P), critic (C), arbiter (A).
# roles_allowed guard (hydramind/rosters.py): grok=critic-only, dsv4-flash=critic-only.
dev:
  proposer: mistral
  critic: dsv4-flash
  arbiter: claude-haiku

# frontier — All-Anthropic eval roster (Track B). Cheap Sonnet proposer+critic,
# Opus only as the deciding arbiter. Legal: opus/sonnet carry no ROLES_ALLOWED
# restriction. Spends real proxy $ (2x sonnet + 1x opus/claim) — eval-only.
frontier:
  proposer: claude-sonnet
  critic: claude-sonnet
  arbiter: claude-opus

# prod — the Phase 3 rerun roster (jackie, final, 2026-07-22): Opus proposes
# on the FREE Lane-Worker seat (opus-worker → L-W, subscription auth), Grok
# criticizes (critic-only guard holds), GPT arbitrates. DeepSeek is DUMPED
# from the prod panel (supersedes the earlier [grok, dsv4-flash] critic pool).
prod:
  proposer: opus-worker
  critic: grok-4.3
  arbiter: gpt-5.5
